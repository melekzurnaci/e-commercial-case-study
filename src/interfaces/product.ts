export interface ProductTypes {
  id: number;
  name: string;
  price: number;
  stock: number;
  description: string;
}

export interface ProductEntity {
  id: number;
  name: string;
  price: number;
  stock: number;
  description: string;
  productNumber: string;
}
